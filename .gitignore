# Editor and IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Backup files
*.bak
*.backup

# Test files
test-output/
*.test

# Secrets and passwords
# VNC password file (contains plain text password)
hooks/.vnc-password
.vnc-password
mounted-files/root/.ssh/github_id_ed25519

# Credential scripts (user-specific)
hooks/.credential-scripts
hooks/credential-scripts/

# Generated and runtime files
generated/

# User-specific config that may contain secrets
# NOTE: hooks/config/icewm/ is explicitly tracked (no secrets)
# Add specific paths here if future config dirs contain secrets

# Python cache
__pycache__/
*.pyc
tests/__pycache__/
mounted-files/root/.ssh/authorized_keys


*.lock
*.log
*.old
*.pid

.Trash-0/

# Runtime artifacts in mounted-files (generated at runtime, not config)
mounted-files/root/.Xauthority
mounted-files/root/.bash_history
mounted-files/root/.bashrc
mounted-files/root/.zshrc
mounted-files/root/.common_shrc
mounted-files/root/.xsession-errors
mounted-files/root/.cache/
mounted-files/root/.local/
mounted-files/root/.mozilla/
mounted-files/root/.npm/
mounted-files/root/.rustup/
mounted-files/root/.tldr/
mounted-files/root/.dbus/
mounted-files/root/.config/dconf/
mounted-files/root/.config/glow/
mounted-files/root/.config/ibus/
mounted-files/root/.config/mlflow/
mounted-files/root/.docker/.token_seed
mounted-files/root/.docker/buildx/
mounted-files/root/.docker/config.json
mounted-files/root/.icewm/prefoverride
mounted-files/root/.mars/
mounted-files/root/.ssh/known_hosts
mounted-files/root/.claude.json
mounted-files/root/.claude/
mounted-files/root/.wezterm.lua
